<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<meta http-equiv="x-ua-compatible" content="ie=edge">
	<title>Futiba Club - Fullstack Academy - DevPleno</title>
	<!-- Font Awesome -->
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
	<!-- Bootstrap core CSS -->
	<link href="/css/bootstrap.min.css" rel="stylesheet">
	<!-- Material Design Bootstrap -->
	<link href="/css/mdb.min.css" rel="stylesheet">
	<!-- Your custom styles (optional) -->
	<link href="/css/style.css" rel="stylesheet">
	<link href="/css/futiba-interno.css" rel="stylesheet">
</head>

<body>
	<header>
		<!--Navbar-->
		<nav class="navbar navbar-expand-lg navbar-light green fixed-top scrolling-navbar">
			<% include ../elements/menu-items %>
		</nav>
	</header>

	<section class="content">
		<div class="container">
			<div class="row">
				<p class="col h5 text-center mb-4">Gerenciamento de grupos</p>
			</div>
		</div>
	</section>

	<section class="group">
		<div class="container">
			<div class="row">
				<div class="col">
					<% if(pendings.length > 0) { %>
					<table class="table table-striped">
						<thead>
							<tr>
								<th>Usuário</th>
								<th>E-mail</th>
								<th>Ações</th>
							</tr>
						</thead>
						<tbody>

							<% pendings.forEach( pending => { %>
							<tr>
								<td scope="row"><%= pending.nome %></td>
								<td scope="row"><%= pending.email %></td>
								<td>
									<% if(user.role) { %>
									<a href="/groups/<%= pending.id %>/pending/yes">Aprovar</a>
									<a href="/groups/<%= pending.id %>/pending/no">Rejeitar</a>
									<% } %>
								</td>
							</tr>
							<% }) %>
							
						</tbody>
					</table>
					<% } else { %>
						<p class="alert alert-info">Não existem usuários pendentes de aprovação para acesso ao grupo</p>
					<% } %>
				</div>

			</div>
		</div>
	</section>

	<% include ../elements/footer %>
<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<meta http-equiv="x-ua-compatible" content="ie=edge">
	<title>Futiba Club - Fullstack Academy - DevPleno</title>
	<!-- Font Awesome -->
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
	<!-- Bootstrap core CSS -->
	<link href="/css/bootstrap.min.css" rel="stylesheet">
	<!-- Material Design Bootstrap -->
	<link href="/css/mdb.min.css" rel="stylesheet">
	<!-- Your custom styles (optional) -->
	<link href="/css/style.css" rel="stylesheet">
	<link href="/css/futiba-interno.css" rel="stylesheet">
</head>

<body>
	<header>
		<!--Navbar-->
		<nav class="navbar navbar-expand-lg navbar-light green fixed-top scrolling-navbar">
			<% include ../elements/menu-items %>
		</nav>
	</header>

	<section class="content">
		<div class="container">
			<div class="row">

				<form class="col" method="POST">
					<p class="h5 text-center mb-4">Gerenciamento de grupos</p>
					<div class="md-form">
						<i class="fa fa-user prefix grey-text"></i>
						<input type="text" class="form-control" name="group_name" autofocus>
						<label for="orangeForm-email">Nome do grupo</label>
					</div>

					<div class="text-center">
						<button class="btn btn-deep-orange">Criar grupo</button>
					</div>
				</form>

			</div>
		</div>
	</section>

	<section class="groups">
		<div class="container">
			<div class="row">
				<div class="col">
					<% if(groups.length > 0) { %>
					<table class="table table-striped">
						<thead>
							<tr>
								<th>#</th>
								<th>Grupos</th>
								<th>Role</th>
								<th>Ações</th>
							</tr>
						</thead>
						<tbody>

							<% groups.forEach( group => { %>
							<tr>
								<th scope="row"><%= group.id %></th>
								<td>
									<% if(group.role === 'owner') { %>
									<a href="/groups/<%= group.id %>"><%= group.name %></a>
									<% } else { %>
										<%= group.name %>
									<% } %>
								</td>
								<td><%= group.role %></td>

								<td>
									<% if(group.role === 'owner') { %>
									<a href="/groups/delete/<%= group.id %>">
										Excluir
									</a>
									<% } else { %>
										<a href="/groups/<%= group.id %>/join">
											Solicitar entrada
										</a>
									<% } %>
								</td>

							</tr>
							<% }) %>
							
						</tbody>
					</table>
					<% } else { %>
						<p class="alert alert-info">Nenhum grupo cadastrado</p>
					<% } %>
				</div>

			</div>
		</div>
	</section>

	<% include ../elements/footer %>